applications:
  - telegraf
classes:
 - service.telegraf.support
parameters:
  telegraf:
    remote_agent:
      enabled: true
      interval: 15
      round_interval: false
      metric_batch_size: 1000
      metric_buffer_limit: 10000
      collection_jitter: 2
